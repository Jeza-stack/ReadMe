import { PillarIndex } from '@/components/PillarPages';

export const metadata = {
  title: 'AI for Students',
  description:
    'Fundamentals, prompt craft, and academic integrity — use AI honestly and well in your studies.',
};

export default function AiForStudentsPage() {
  return <PillarIndex pillar="ai-for-students" />;
}
